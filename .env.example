# This file serves as an example for environment variable configuration.
# Copy this file to .env and fill in the required values.

# FastAPI application settings
API_HOST=0.0.0.0
API_PORT=8000
API_V1_STR="/api/v1"

# Hugging Face model settings
TEXT_TO_VECTOR_MODEL_NAME="sentence-transformers/all-MiniLM-L6-v2"
# Example: Other model names if you want them configurable
# SENTIMENT_MODEL_NAME="distilbert-base-uncased-finetuned-sst-2-english"
# EMOTION_MODEL_NAME="cardiffnlp/twitter-roberta-large-emotion-latest"

# Hugging Face Token (optional, if using private models)
HF_TOKEN=""

# Security settings
API_SECRET="" # Leave empty for no API key authentication

# Logging settings
LOG_LEVEL="INFO" # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# Performance and operational settings
DEBUG_MODE=True # Set to True for development (enables reload, more verbose logs)
REQUEST_TIMEOUT_SECONDS=100 # Max time for a request to be processed
RATE_LIMIT_REQUESTS=100 # Max requests per window
RATE_LIMIT_WINDOW_SECONDS=60 # Time window for rate limit in seconds
DEFAULT_MODEL_QUANTIZATION=True # Whether to quantize models by default (can improve performance)

TEXT_GENERATION_MODEL_REPO ="allenai/OLMo-2-0425-1B-Instruct-GGUF"
TEXT_GENERATION_MODEL_FILE ="OLMo-2-0425-1B-Instruct-Q4_K_M.gguf"
TEXT_GENERATION_CONTEXT_LENGTH = 4096

GEMINI_API_KEY=""